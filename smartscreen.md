# 任务：智慧舆情功能模块（后端 + 前端）

## 一、模块概述

- 定位：在已完成“智能瞭望系统”（任务1）和“智能聊天系统”（任务2）的基础上，构建一个统一的智慧舆情中心。
- 核心价值：为管理员提供全局态势感知（数智大屏） + 智能风险分析（AI舆情）。
- 数据来源：
  - 瞭望子系统：设备状态、告警事件、爬虫采集的结构化数据。
  - 聊天子系统：用户消息（1v1/群聊）、文件上传记录、用户在线/活跃数据。
- 依赖关系：
  - 前端需集成 ECharts GL（3D地球）、Wordcloud2、ECharts 常规图表。
  - 后端需调用 AI 模型接口（如川农小助手所在的大模型）进行自然语言分析。
  - 本模块的所有数据存储必须支持 MySQL 和 SQLite 双数据库，并符合“任务5”的切换要求。

## 二、数智大屏功能（前端核心）

### 2.1 3D 地球可视化层

- 参数配置（前端）：
  - `earth_auto_rotate`：布尔值，地球是否自动缓慢旋转，默认 `true`。
  - `earth_rotate_speed`：浮点数，旋转速度（弧度/秒），默认 `0.005`。
  - `point_display_max`：整数，地球上同时显示的最大标记点数，默认 `500`，超出则聚合显示。
- 数据映射规则：
  - 瞭望设备标记：从后端接口获取设备列表（含经纬度、设备名、告警级别）。  
    - 绿色光柱：设备在线且无告警。  
    - 黄色光柱：设备在线且有轻微告警。  
    - 红色脉冲球：设备离线或严重告警。  
  - 聊天用户活跃标记：从后端获取各地理位置的在线用户数。  
    - 以“流动粒子”形式从用户所在地理位置向外扩散，粒子密度 = 在线用户数 / 10（最多 50 个粒子）。
- 交互要求：
  - 支持鼠标拖拽旋转地球、滚轮缩放。
  - 点击任意标记点，弹出一个 Tooltip，显示：设备名/地区、告警数/在线人数、最后活跃时间。
  - 点击标记点后，右侧统计看板应自动筛选并高亮与该标记相关的数据（可选高级功能）。

### 2.2 数据可视化统计看板

- 布局：位于屏幕左侧或底部，采用半透明深色卡片，支持最小化/展开。
- 必选图表组件：

#### 2.2.1 全局统计卡片（实时或准实时，刷新间隔 ≤ 5 秒）
- 来自瞭望系统：
  - 当前在线设备总数
  - 今日新增告警数
  - 未处理高危事件数
- 来自聊天系统：
  - 总注册用户数
  - 当前在线用户数
  - 今日新增消息数（区分 1v1 和群聊）
  - 今日新增文件数

#### 2.2.2 趋势折线图（面积图）
- 数据源：过去 24 小时或过去 7 天（可选切换）。
- 展示内容：
  - 消息量趋势（蓝色线，左 Y 轴）
  - 瞭望事件发生量趋势（红色线，右 Y 轴）
- 交互：鼠标悬浮显示具体时刻的消息数和事件数。

#### 2.2.3 排行柱状图 / 列表
- 热门群聊排行（按过去 1 小时消息量降序，取前 10）。
- 高频敏感词排行（需结合智能舆情分析结果，按出现次数降序）。
- 文件传输类型分布（饼图或水平柱状图）：图片、文档、压缩包、其他。

### 2.3 词云（Wordcloud）

- 参数配置（后端可调）：
  - `wordcloud_update_interval`：整数，词云数据更新间隔（秒），默认 `60`。
  - `max_words`：整数，词云中最多显示的词语数量，默认 `100`。
  - `exclude_stopwords`：布尔值，是否过滤常见停用词（如“的、了、是”），默认 `true`。
- 数据来源：
  - 从聊天子系统获取所有公开/可分析的群聊及 1v1 消息文本（排除系统命令如 `/help`）。
- 更新频率：按照 `wordcloud_update_interval` 定时请求后端接口。
- 视觉增强：
  - 字体大小 = 词频权重。
  - 颜色规则：
    - 红色：该词出现在敏感词库中，或 AI 识别为高危。
    - 橙色：出现在预警词库中。
    - 蓝色/绿色：中性/积极词汇。
  - 鼠标悬停时显示：词频次数 + 示例消息（脱敏，如“用*代替用户名”）。
- 技术实现：前端使用 `wordcloud2.js` 或 `echarts-wordcloud`。

## 三、智能舆情分析功能（后端 AI + 前端展示）

### 3.1 AI 自主分析引擎（后端服务）

- 触发方式：
  - 定时触发：可配置 cron 表达式（例如 `0 */5 * * * *` 每5分钟一次）。
  - 手动触发：通过后台管理界面按钮调用。
- 分析对象（时间窗口）：
  - 增量分析：仅分析自上次分析以来新增的数据。
  - 全量回溯：管理员可选指定时间范围（如 `last_24h`）。
- 分析数据范围：
  - 聊天文本（消息内容、群公告、用户昵称中的关键词）。
  - 瞭望告警文本（告警标题、描述、设备名）。
- 调用 AI 模型：
  - 使用与“川农小助手”相同的大模型接口（HTTP API 或本地 LLM）。
  - 必须使用专门设计的舆情分析提示词模板（见附录 A）。
- 分析输出结构（JSON）：

```json
{
  "analysis_time": "2026-05-29T15:30:00Z",
  "time_range": "2026-05-29T15:25:00Z - 2026-05-29T15:30:00Z",
  "sentiment": {
    "positive_ratio": 0.65,
    "neutral_ratio": 0.25,
    "negative_ratio": 0.10
  },
  "hot_topics": [
    {"topic": "服务器延迟", "mentions": 45, "summary": "多个用户反映响应变慢"},
    {"topic": "新功能反馈", "mentions": 30, "summary": "大部分为正面评价"}
  ],
  "risks": [
    {
      "risk_level": "high",
      "type": "负面舆论集中",
      "description": "群聊 'A组开发群' 中出现大量关于'文件丢失'的抱怨，情绪激烈",
      "affected_groups": ["A组开发群", "客服群"],
      "sample_messages": ["用户1: 我上传的文件不见了！", "用户2: 我也是，这系统太烂了"]
    }
  ],
  "overall_risk_index": 72
}
```

- 存储要求：每次分析结果保存到数据库表 `opinion_analysis_log`，用于历史查询和趋势展示。

### 3.2 前端舆情展示界面

#### 3.2.1 风险仪表盘（悬浮面板，默认收折在右侧）
- 显示字段：
  - 当前综合风险指数（0-100），用进度条和颜色（绿→黄→红）展示。
  - 最近一次分析时间。
  - 风险指数变化趋势（简单箭头 + 数值差，如上期 +5）。
- 风险指数阈值告警：
  - 当 `overall_risk_index > 70` 时，仪表盘背景闪烁红色边框。
  - 同时触发任务4（语音播报） 的事件：`window.dispatchEvent(new CustomEvent('riskAlert', { detail: { index: 72 } }))`。

#### 3.2.2 风险警示列表
- 展示内容：从上一步 AI 输出的 `risks` 数组中渲染。
- 每条风险项包含：
  - 风险等级徽章（高/中/低）。
  - 风险类型（如“负面舆论集中”、“敏感词爆发”、“异常行为”）。
  - 描述文本。
  - 涉及的主要群聊或设备。
  - 操作按钮：“查看详情”。
- 详情弹窗：
  - 展示 AI 生成的完整分析报告（Markdown 格式渲染）。
  - 展示关联的原始数据样本（最多 10 条聊天记录或告警记录，脱敏处理）。
  - 提供“忽略此风险”按钮（记录到数据库，不再重复提醒）。

#### 3.2.3 “AI 解读”按钮（置于大屏右上角）
- 点击行为：向后端发送请求，要求 AI 对当前大屏展示的所有统计数据生成一段自然语言总结。
- 后端实现：
  - 收集当前全局统计卡片、趋势图最后 10 个点、排行前 3 项等数据。
  - 调用 AI 模型，使用“大屏解读提示词”（见附录 B）。
  - 返回文本。
- 前端展示：以 Toast 或底部抽屉消息展示，例如：“根据当前数据，过去一小时消息量上升 15%，但告警事件同步增加，主要集中在华东区域，建议关注设备稳定性。”

### 3.3 与任务2（智能聊天子系统）的联动 - AI 数字员工支持舆情问答

- 允许的角色：仅管理员或具有权限的用户可以在聊天子系统内 @川农小助手 询问舆情。
- 支持的问题示例：
  - “当前整体舆情风险怎么样？”
  - “最近一小时有什么负面消息吗？”
  - “帮我总结一下过去24小时的热点话题。”
  - “风险最高的群聊是哪个？”
- 实现逻辑：
  - 聊天机器人收到消息后，识别出意图为“舆情查询”。
  - 调用本模块的 `/api/opinion/latest` 获取最新分析结果。
  - 将结果组装成自然语言回复（可使用简单模板或再次调用 LLM 润色）。
  - 多轮对话要求：能够记住用户上一次问的是“风险指数”，下一次问“具体是哪些群”时能正确关联。

## 四、后端 API 接口定义（供前后端对接）

> 以下接口遵循 RESTful 风格，返回 JSON，错误时使用标准 HTTP 状态码 + `{"error": "message"}`。

### 4.1 获取大屏基础数据

- 端点：`GET /api/opinion/dashboard`
- 查询参数：无（或 `?refresh=true` 强制刷新缓存）
- 响应示例：

```json
{
  "stats": {
    "online_devices": 42,
    "alerts_today": 7,
    "critical_events": 2,
    "total_users": 1230,
    "online_users": 89,
    "messages_today": 3421,
    "files_today": 56
  },
  "trend": {
    "timestamps": ["15:00","15:05",...],
    "messages": [120,135,...],
    "events": [3,2,...]
  },
  "rankings": {
    "hot_groups": [{"name":"闲聊群","msg_count":230},...],
    "sensitive_words": [{"word":"崩溃","count":12},...]
  },
  "file_type_dist": {"image": 30, "doc": 15, "other": 11}
}
```

### 4.2 获取词云数据

- 端点：`GET /api/opinion/wordcloud`
- 查询参数：
  - `limit`：整数，最多返回多少词，默认 100。
  - `since`：时间戳，可选，限制从该时间之后的消息统计。
- 响应：

```json
[
  {"word": "服务器", "weight": 230, "risk": false},
  {"word": "卡顿", "weight": 98, "risk": true},
  ...
]
```

### 4.3 获取最新 AI 舆情分析结果

- 端点：`GET /api/opinion/latest`
- 响应：与 3.1 中 `分析输出结构` 一致。

### 4.4 手动触发 AI 分析

- 端点：`POST /api/opinion/analyze`
- 请求体：`{"time_range": "last_30m"}` 或 `{"start": "2026-05-29T10:00:00Z", "end": "2026-05-29T11:00:00Z"}`
- 响应：返回本次分析结果（同上结构），并异步保存到数据库。

### 4.5 AI 解读大屏数据

- 端点：`POST /api/opinion/explain`
- 请求体：包含当前大屏的所有关键统计摘要（由前端收集后发送）。
- 响应：

```json
{
  "summary": "根据过去一小时数据，消息量较上一小时增加12%，告警事件主要集中在设备组A，建议检查网络连接。"
}
```

## 五、数据库设计（需同时支持 SQLite 和 MySQL）

### 表名：`opinion_analysis_log`

| 字段名             | 类型        | 说明                                         |
|-------------------|-------------|----------------------------------------------|
| id                | INTEGER PK  | 自增主键                                     |
| analysis_time     | DATETIME    | 分析时间                                     |
| time_range_start  | DATETIME    | 分析窗口起始                                 |
| time_range_end    | DATETIME    | 分析窗口结束                                 |
| sentiment_json    | TEXT        | 存储情绪比例 JSON                            |
| hot_topics_json   | TEXT        | 存储热点话题 JSON                            |
| risks_json        | TEXT        | 存储风险列表 JSON                            |
| overall_risk_index| INTEGER     | 0-100 风险指数                               |
| raw_ai_response   | TEXT        | 可选，保存大模型原始返回，用于调试            |

### 表名：`opinion_ignored_risks`

| 字段名         | 类型        | 说明                         |
|----------------|-------------|------------------------------|
| id             | INTEGER PK  |                              |
| risk_hash      | VARCHAR(64) | 风险的唯一标识（如内容摘要的MD5） |
| ignored_by     | VARCHAR(50) | 操作人用户名                 |
| ignored_at     | DATETIME    | 忽略时间                     |

## 六、非功能性要求

- 性能：
  - 大屏数据 API 响应时间 ≤ 200ms（需使用缓存，缓存时间 ≤ 5 秒）。
  - 词云数据 API 响应时间 ≤ 500ms。
  - AI 分析接口（手动触发）允许异步执行，前端轮询获取结果，但总耗时不超过 30 秒。
- 安全：
  - 所有舆情 API 必须验证用户角色为 `admin` 或 `operator`。
  - 日志中不得记录原始聊天消息原文（需脱敏）。
- 容错：
  - 若 AI 模型接口超时或返回错误，系统应使用上一次分析结果，并在日志中记录。
  - 定时舆情分析任务失败时，应重试 3 次，间隔 1 分钟，仍失败则发送告警到管理群（复用任务3的群通知能力）。
- 数据库切换：所有涉及的表均需提供 SQLite 和 MySQL 两种 DDL，并在应用启动时根据配置自动创建/迁移。

## 七、交付物清单（供 AI 生成参考）

1. 后端代码：
   - 舆情分析调度器（APScheduler 或 Celery Beat）
   - API 控制器：`OpinionController`
   - AI 调用封装类（含提示词模板）
   - 数据访问层（支持多数据库）
2. 前端页面/组件：
   - `OpinionDashboard.vue`（或 React 等价）
   - 3D Earth 组件（基于 ECharts GL）
   - 词云组件
   - 风险仪表盘组件
3. 数据库迁移脚本：针对 SQLite 和 MySQL 的初始化 SQL。
4. 测试用例：至少覆盖手动触发分析、获取大屏数据、风险阈值告警事件触发。

## 附录 A：舆情分析提示词模板（示例）

```
你是一个网络舆情分析专家。请分析以下聊天记录和告警数据，并严格按照 JSON 格式输出结果。
不要输出任何解释，只输出 JSON。

聊天数据样例：
{chat_samples}

告警数据样例：
{alert_samples}

输出格式：
{
  "sentiment": {"positive_ratio": float, "neutral_ratio": float, "negative_ratio": float},
  "hot_topics": [{"topic": string, "mentions": int, "summary": string}],
  "risks": [{"risk_level": "high|medium|low", "type": string, "description": string, "affected_groups": [string], "sample_messages": [string]}],
  "overall_risk_index": int
}

注意：
- 风险指数为 0-100，超过 70 为高风险。
- 如果发现攻击性、政治敏感或极度负面言论，提高风险等级。
```

## 附录 B：大屏解读提示词模板（示例）

```
你是一个数据分析师。请根据以下统计摘要，用一段自然语言（不超过 100 字）总结当前系统态势。

统计摘要：
{stats_summary}

要求：
- 指出明显的变化趋势（上升/下降）。
- 如果有风险点，提一句。
- 语气中立、专业。
```
